import { defineCliConfig } from "sanity/cli";

export default defineCliConfig({
  api: {
    projectId: "qasd0k09",
    dataset: "production",
  },
  studioHost: "frederico-theophilo-neto",
  autoUpdates: true,
});
